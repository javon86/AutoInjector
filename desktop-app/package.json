{
  "name": "autoinjector-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop app: ChatGPT, Claude and Gemini as three real browser panes in one window, roundtabling a topic via browser copy/paste automation (no API keys).",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^43.0.0"
  }
}
